# SPA fallback moved to functions/_middleware.ts — Cloudflare's
# _redirects parser flags `/* /index.html 200` as an "infinite loop"
# and silently ignores it, so direct hits to client-side routes 404.
# The middleware does the same rewrite explicitly. Keeping this
# file empty (rather than deleting) so future redirects have a home.
