/**
 * Compatibility shim — Tab moved to the shared trader UI module.
 * Re-exports `@/components/ui` so existing imports keep working.
 *
 * New code should import directly:
 *
 *   import { Tab } from "@/components/ui";
 */
export { Tab } from "@/components/ui";
